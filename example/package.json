{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "actions-on-google": "^1.0.9",
    "reflect-metadata": "^0.1.10",
    "voice-assistant-js": "file:/Users/caziel/workspace/own/voice-assistent-js"
  },
  "scripts": {
    "deploy": "rm -rf node_modules/* && yarn install --force --production && npm run package && npm run upload",
    "package": "zip -r package.zip .",
    "upload": "aws lambda update-function-code --function-name sonos --zip-file fileb://package.zip"
  }
}

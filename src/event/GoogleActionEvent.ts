import {IEvent} from '../../typings'
import {GoogleEvent} from './GoogleEvent'

export class GoogleActionEvent extends GoogleEvent implements IEvent {
  /*
   * buildInputPrompt
   * getActionVersionLabel
   * getConversationId
   * getDialogState
   */

}
